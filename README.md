# Semantic-Drift-Before-Inference-Attacks
Demonstrates a security issue in Vision–Language Models caused by standard image preprocessing. It shows how hidden content invisible to humans can become visible to AI models after preprocessing and introduces a method to detect such inconsistencies.
